import React from "react";

const IconTitle = ({ title }) => {
	return (
		<h2 className="text-xl md:text-2xl lg:text-3xl text-orange font-bold tracking-wider border-b border-paghoverbg pb-2 mt-4 mb-8">
			{title}
		</h2>
	);
};

export default IconTitle;
