import React from "react";

const IconTitle = ({ title }) => {
	return (
		<h2 className="text-3xl text-orange font-semibold tracking-wider border-b border-paghoverbg pb-2 mt-4 mb-8">
			{title}
		</h2>
	);
};

export default IconTitle;
